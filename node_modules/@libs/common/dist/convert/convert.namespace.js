"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var Convert;
(function (Convert) {
    Convert.int = (value, defaultValue) => {
        if (!value) {
            if (defaultValue)
                return defaultValue;
            else
                throw new Error(`Value is empty while no default value provided.`);
        }
        const parsed = parseInt(value, 10);
        if (isNaN(parsed)) {
            throw new Error(`Value <${value}> can't be converted to integer.`);
        }
        return parsed;
    };
    Convert.float = (value, defaultValue) => {
        if (!value) {
            if (defaultValue)
                return defaultValue;
            else
                throw new Error(`Value is empty while no default value provided.`);
        }
        const parsed = +value;
        if (isNaN(parsed)) {
            throw new Error(`Value <${value}> can't be converted to fload.`);
        }
        return parsed;
    };
})(Convert = exports.Convert || (exports.Convert = {}));
//# sourceMappingURL=convert.namespace.js.map