"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const get_format_1 = require("./get-format");
const parse_1 = require("./parse");
exports.readObj = (pathToFile) => {
    if (!path.isAbsolute(pathToFile))
        pathToFile = path.join(__dirname, pathToFile);
    return new Promise((resolve, reject) => {
        fs.readFile(pathToFile, (error, content) => {
            if (error)
                reject(error);
            else {
                const obj = parse_1.parse(content.toString(), get_format_1.getFormat(pathToFile));
                resolve(obj);
            }
        });
    });
};
//# sourceMappingURL=read-obj.js.map