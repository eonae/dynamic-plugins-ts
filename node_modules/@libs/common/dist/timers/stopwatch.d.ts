export declare type HrTime = [number, number];
export declare class Stopwatch {
    private _start;
    private _current;
    private _isRunning;
    readonly elapsedMilliseconds: number;
    readonly elapsed: HrTime;
    reset(): Stopwatch;
    restart(): Stopwatch;
    start(): Stopwatch;
    pause(): Stopwatch;
    private convertToMilliseconds;
}
